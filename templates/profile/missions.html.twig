{% extends 'base.html.twig' %}

{% block title %}Hello ProfileController!{% endblock %}

{% block body %}
    <div class="profile-patron">
        {% for mission in missions %}
            <p>{{ mission.descriptif }}</p>
            <p>{{ mission.date_debut|format_datetime('medium', 'short') }}</p>
            <p>{{ mission.date_fin|format_datetime('medium', 'short') }}</p>
            <p>{{ mission.date_facture|format_datetime('medium', 'short') }}</p>
            <p>{{ mission.montant }}</p>
        {% endfor %}
    </div>
{% endblock %}
